[tox]
envlist = py27, flake8, sphinx, readme

[testenv]
deps =
    coverage
    pytest
    pytest-cov
    pytest-xdist
commands =
    pip install -e .[ssh]
    py.test -x --verbose --capture=no --cov=xal --doctest-modules {posargs: xal tests}
    coverage erase
    pip freeze

[testenv:flake8]
deps =
    flake8
commands =
    flake8 xal

[testenv:sphinx]
deps =
    Sphinx
commands =
    pip install -e .[ssh]
    make --directory=docs SPHINXOPTS='-W' clean {posargs:html doctest linkcheck}
whitelist_externals =
    make

[testenv:readme]
deps =
    docutils
    pygments
commands =
    mkdir -p var/docs
    rst2html.py --exit-status=2 README.rst var/docs/README.html
    rst2html.py --exit-status=2 CONTRIBUTING.rst var/docs/CONTRIBUTING.html
whitelist_externals =
    mkdir

[testenv:release]
deps =
    wheel
    zest.releaser
commands =
    fullrelease
